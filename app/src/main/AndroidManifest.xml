<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mega.project_bimbingan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".navigation.navigation_dosen"></activity>
        <activity android:name=".Mhs_ajukan_janji" />
        <activity android:name=".fragment.UserFragment" />
        <activity android:name=".ui.dosen_jadwal_temu_mhs" />
        <activity android:name=".ui.notification"
            android:parentActivityName=".fragment.HomeFragment"
            ></activity>
        <activity android:name=".ui.jadwal_ketemu_dosen" />
        <activity android:name=".ui.Mhs_subscribe" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.dosen_note" />
        <activity android:name=".listDosen" />
        <activity android:name=".dosen_addNote" />
        <activity android:name=".setStatus" />
        <activity android:name=".navigation.navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>